#include <bits/stdc++.h>
using namespace std;

int main()
{
    double number;
    cout << "Give the number: ";
    cin >> number;
    cout << "Floor of " << number << " is " << floor(number) << endl;
    cout << "Ceil of " << number << " is " << ceil(number) << endl;
    return 0;
}